void main() {
  //To explicitly declare the data type of the list we are using
  List<String> names = ['ruby', 'seun', 'joshua'];
  print(names);
  // This list can only allow string varibles

  //A better way will be
  var Newnames = <String>['seun', 'favour'];
  print(Newnames);
}
